node_modules/
dist/
*.js.map
*.d.ts.map

# Server data
server/data/
data-dev/
server/data-dev/

# Vite
client/dist/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Claude Code local settings
.claude/

# Planning docs (used in PRs, not tracked)
docs/brainstorms/
docs/plans/

# Environment
.env
.env.local

# TypeScript incremental build cache (machine-specific; must not be committed)
*.tsbuildinfo

# argusd daemon binaries (built from daemon/ via `make build` in CI/packaging)
electron/resources/argusd/argusd-arm64
electron/resources/argusd/argusd-x64

# Go build output (default `go build` in daemon/ emits ./daemon)
daemon/daemon
