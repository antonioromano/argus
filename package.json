{
  "name": "argus",
  "version": "0.16.48",
  "description": "Dashboard for managing multiple Claude Code CLI sessions",
  "author": "Antonio Romano <antonio@rebrandly.com>",
  "private": true,
  "type": "module",
  "license": "MIT",
  "main": "electron/dist/main.js",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "postinstall": "node scripts/postinstall.js",
    "dev": "npm run electron:dev",
    "dev:web": "PORT=5401 DATA_DIR=data-dev concurrently -n server,client -c blue,green \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w shared && npm run build -w server && npm run build -w client",
    "build:electron": "tsc -p electron/tsconfig.json",
    "build:all": "npm run build -w shared && npm run build -w server && npm run build -w client && npm run build:electron",
    "electron:dev": "npm run build:all && ARGUS_PORT=5403 electron .",
    "package:mac": "npm run build:all && npx electron-builder install-app-deps && electron-builder --mac -p never --config electron-builder.config.cjs"
  },
  "dependencies": {
    "@xterm/headless": "^6.0.0",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "fix-path": "^4.0.0",
    "node-pty": "^1.1.0",
    "semver": "^7.7.1",
    "socket.io": "^4.8.3",
    "uuid": "^11.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "typescript": "^5.7.0",
    "electron": "^33.0.0",
    "electron-builder": "^25.0.0"
  }
}
